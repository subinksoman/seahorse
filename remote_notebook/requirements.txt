# Seahorse notebook image extras — Python 3.12.
#
# The base image (quay.io/jupyter/minimal-notebook:python-3.12) already ships the modern
# Jupyter stack: JupyterLab 4, Notebook 7, jupyter-server 2, jupyter-client 8, jupyter-core 5,
# ipykernel 6, nbconvert 7, nbformat 5, tornado 6, traitlets 5, pyzmq. We only add what the
# Seahorse custom kernels / contents manager / server extension need on top of that base.
#
# The ancient pins that used to live here (ipykernel==4.5.0, jupyter-client==4.4.0,
# classic notebook, tornado==4.4.2, prompt-toolkit==1.0.7, pika==0.11.2, nbconvert==4.2.0,
# nbformat==4.1.0, jupyterhub==0.5.0, …) were all Python-3.7-era and incompatible with
# Jupyter Server 2 / Python 3.12; they are dropped in favour of the base image's versions.

# RabbitMQ bridge used by the forwarding kernels (socket_forwarder / rabbit_mq_client).
# pika 1.3.x is the current line and keeps the BlockingConnection API the bridge relies on.
pika==1.3.2

# The WMContentsManager talks to workflowmanager over HTTP.
requests
